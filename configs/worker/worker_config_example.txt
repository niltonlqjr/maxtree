
# comments will be ignored, and every comment must start the line with # (it can't have any char before, not even a blank space)
# blank lines also be ignored
# the args names (lvalue) and values (rvalue) are preprocessed by a right trim and left trim

# number of threads for the process 
threads=6


# print some information(true/false)
verbose=false
# print information with colors (true/false)
colored = false

# main server (IP address)
server_ip = 127.0.0.1

#server_recv_port: port that manager uses to receive from workers (to registry at server and send messages)
server_recv_port = 7233

#server_send_port: port that manager uses to send message to workers (to receive messages from server)
server_send_port = 7234

#lambda: atribute value to filter
lambda=100000

#input: input filename filename
input=../testes/dos_wp_bw.png 
#input = ../testes/example_gotz.png

#output: output filename prefix
output=../output/parallel/example/example_out

#output_ext: output extension format (only the libvips extensions)
output_ext=png

#join_image: generate the full image joining the tiles
#join_image=true
join_image=false

#hw_specs: filename that store hardware specifications
hw_specs = ../hardware.yaml


#hostname: a string that will be stored on manager to comunication ends
#this hostname will be complemented with pid 
hostname = test_host